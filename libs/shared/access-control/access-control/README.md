# shared-access-control-access-control

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-access-control-access-control` to build the library.

## Running unit tests

Run `nx test shared-access-control-access-control` to execute the unit tests via [Jest](https://jestjs.io).
